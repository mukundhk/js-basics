// null is given by programmer
// undefined is given by program

// map syntax
// iterable.map(function)

const cowsay = require("cowsay");

console.log(
  cowsay.say({
    text: "I'm a moooodule",
    e: "👁👁",
    T: "🦷🦷",
  })
);
